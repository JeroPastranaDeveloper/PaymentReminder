package com.pr.paymentreminder.data.model

enum class ServiceItem {
    NAME,
    CATEGORY,
    DATE,
    TYPE,
    PRICE,
    REMEMBER
}

val nameItem = ServiceItem.NAME
val dateItem = ServiceItem.DATE
val categoryItem = ServiceItem.CATEGORY
val priceItem = ServiceItem.PRICE
val typeItem = ServiceItem.TYPE
val rememberItem = ServiceItem.REMEMBER
